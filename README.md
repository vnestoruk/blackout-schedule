# ⚡ Blackout Schedule - Графік відключень електроенергії

Web-додаток для відстеження графіків планових відключень електроенергії в Івано-Франківську.

## 🌟 Особливості

- 🎨 **Чистий інтерфейс** - фокус на важливому, налаштування в модальному вікні
- 🌓 **Три теми** - світла, темна та авто (слідує за системою), з легким перемиканням
- 📅 **Багатоденні графіки** - всі доступні дати (сьогодні, завтра і далі) в окремих картках
- 📊 **Детальна аналітика** для кожного дня:
  - Кількість періодів відключень
  - Загальний час без світла (год + хв)
  - Загальний час зі світлом (год + хв)
  - Відсоток часу без світла
  - Візуальна шкала співвідношення
- 📍 **Підписки на черги** - підпишіться на декілька черг одночасно
- ⚙️ **Зручні налаштування** - все в одному місці, одним кліком
- 🌍 **Швидка зміна регіону** - перегляд будь-якого регіону без підписки
- ✅ **Візуальний графік** - таймлайн з червоними/зеленими зонами (за замовчуванням) або список
- 🟢🔴 **Поточний статус** - фон сторінки змінює колір залежно від наявності електроенергії
- ⏱️ **Таймер** - відлік до наступного відключення/відновлення
- 🔔 **Розумні сповіщення** (для всіх підписок):
  - При додаванні нових дат графіка (наприклад, графік на завтра) 📅
  - При зміні графіка (кожні 10 хвилин) 🔄
  - За 15 хвилин до відключення ⚠️
  - За 15 хвилин до відновлення ✅
- 🔄 **Автооновлення** - перевірка змін кожні 10 хвилин
- 🎮 **Опціональна гра** - хрестики-нулики під час відключень (за бажанням)
- 📱 **PWA** - можна встановити як додаток на телефон
- 💾 **Офлайн режим** - працює без інтернету після першого завантаження
- 🎯 **Мультирегіональність** - легко додати нові регіони

## 🚀 Швидкий старт

### Встановлення залежностей

```bash
npm install
```

### Запуск в режимі розробки

```bash
npm run dev
```

Відкрийте [http://localhost:5173](http://localhost:5173) в браузері.

### Збірка для продакшну

```bash
npm run build
```

Результат збірки буде в папці `dist/`.

### Перегляд продакшн збірки локально

```bash
npm run preview
```

## 📦 Деплой

### Варіант 1: Vercel (рекомендується)

1. Завантажте код на GitHub
2. Зайдіть на [vercel.com](https://vercel.com)
3. Натисніть "Import Project"
4. Виберіть ваш репозиторій
5. Vercel автоматично визначить налаштування Vite
6. Готово! 🎉

### Варіант 2: Netlify

1. Збудуйте проект: `npm run build`
2. Зайдіть на [netlify.com/drop](https://app.netlify.com/drop)
3. Перетягніть папку `dist/` в браузер
4. Готово! 🎉

### Варіант 3: GitHub Pages

```bash
npm run build
# Завантажте вміст папки dist/ до вашого GitHub Pages
```

## 🎨 PWA Іконки

Для повноцінної роботи PWA потрібно створити іконки:

1. Створіть іконку 512x512px у форматі PNG
2. Використайте [PWA Image Generator](https://www.pwabuilder.com/imageGenerator)
3. Згенеруйте всі необхідні розміри
4. Замініть файли в папці `public/`:
   - `pwa-192x192.png`
   - `pwa-512x512.png`

## 🔧 Налаштування

### Зміна черги за замовчуванням

Відредагуйте `src/utils/storage.js`:

```javascript
getQueue() {
  return localStorage.getItem(STORAGE_KEYS.QUEUE) || '4.1' // Ваша черга
}
```

### Додавання нових регіонів

Відредагуйте `src/utils/api.js`:

```javascript
export const REGIONS = {
  IF: { ... },
  // Додайте новий регіон
  LVIV: {
    name: 'Львів',
    endpoint: 'https://api-endpoint-for-lviv.com',
    queues: ['1.1', '1.2', ...],
    parseResponse: (data) => {
      // Функція для парсингу даних
      return data
    }
  }
}
```

### Налаштування інтервалу оновлення

За замовчуванням - 10 хвилин. Змінити можна в `src/composables/useSchedule.js`:

```javascript
// Poll every 10 minutes
pollInterval = setInterval(() => {
  fetch(true);
}, 10 * 60 * 1000); // Змініть тут
```

## 🛠️ Технології

- **Vue 3** - прогресивний JavaScript фреймворк
- **Vite** - швидкий інструмент збірки
- **Tailwind CSS** - utility-first CSS фреймворк для швидкої розробки
- **Vite PWA Plugin** - підтримка Progressive Web App
- **Lucide Icons** - красиві SVG іконки
- **Composition API** - сучасний підхід до Vue компонентів

## 📱 Використання як PWA

### На Android/iOS:

1. Відкрийте сайт в Chrome/Safari
2. Натисніть меню (⋮)
3. Виберіть "Додати на головний екран"
4. Додаток з'явиться як звичайна програма

### На Desktop (Chrome/Edge):

1. Відкрийте сайт
2. Натисніть іконку встановлення в адресному рядку
3. Підтвердіть встановлення

## 📊 Структура проекту

```
blackout/
├── public/              # Статичні файли
├── src/
│   ├── components/      # Vue компоненти
│   │   ├── StatusIndicator.vue
│   │   ├── ScheduleView.vue
│   │   ├── QueueSelector.vue
│   │   └── NotificationPrompt.vue
│   ├── composables/     # Логіка (композиції)
│   │   ├── useSchedule.js
│   │   ├── useStatus.js
│   │   └── useNotifications.js
│   ├── utils/           # Утиліти
│   │   ├── api.js
│   │   ├── storage.js
│   │   └── schedule-parser.js
│   ├── App.vue          # Головний компонент
│   ├── main.js          # Точка входу
│   └── style.css        # Глобальні стилі
├── index.html
├── vite.config.js
└── package.json
```

## 🤝 Внесок

Будь-які покращення вітаються! Створюйте Pull Request або Issue.

## ⚙️ Інтерфейс

Чистий, зрозумілий інтерфейс з усіма налаштуваннями в одному місці:

- **Головний екран**: Статус і графік поточної черги
- **Верхня панель**: Регіон та черга, кнопка налаштувань (⚙️)
- **Модальне вікно**: Зміна перегляду, підписки на сповіщення

## 📍 Підписки

Підпишіться на декілька черг одночасно і отримуйте сповіщення для кожної:

- Дім (черга 4.1)
- Офіс (черга 2.1)
- Батьки (черга 3.1)

Все керується через налаштування (⚙️ кнопка).

## 🔔 Сповіщення

Додаток підтримує три типи сповіщень для всіх підписок:

1. **Зміна графіка** - коли енергокомпанія оновлює розклад

2. **Попередження про відключення** - за 15 хв до блекауту
3. **Попередження про відновлення** - за 15 хв до увімкнення світла

## 📄 Ліцензія

MIT

---
